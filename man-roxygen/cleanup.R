#' @param cleanup Remove intermediate files after atlas creation. If not
#'   specified, uses `options("ggseg.extra.cleanup")` or the `GGSEG_EXTRA_CLEANUP`
#'   environment variable. Default is TRUE.
