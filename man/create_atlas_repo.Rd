% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-repo.R
\name{setup_atlas_repo}
\alias{setup_atlas_repo}
\title{Create a new ggseg atlas package}
\usage{
setup_atlas_repo(
  path,
  atlas_name = NULL,
  open = rlang::is_interactive(),
  rstudio = TRUE
)
}
\arguments{
\item{path}{Where to create the package. If the directory exists, it must
be empty.}

\item{atlas_name}{Name of the atlas (lowercase, no spaces). The package
name becomes \verb{ggseg\{AtlasName\}}. If NULL, derived from the directory
name (e.g., path \code{ggsegDkt} becomes atlas name \code{dkt}).}

\item{open}{If TRUE, opens the new project in RStudio. Default is TRUE
when running interactively.}

\item{rstudio}{If TRUE, creates an \code{.Rproj} file for RStudio users.}
}
\value{
Invisibly returns the path to the created package.
}
\description{
Scaffold an R package for distributing a brain atlas. The generated
package follows ggseg conventions and includes everything you need:
template scripts for atlas creation, documentation stubs, a test suite,
and GitHub Actions for automated checking.
}
\details{
The package will be named \verb{ggseg\{AtlasName\}} (e.g., \code{ggsegSchaefer} for
a Schaefer parcellation). After creation, edit the files in \verb{data-raw/}
to build your atlas, then run \code{devtools::document()} and \code{devtools::check()}.
}
\examples{
\dontrun{
# Create atlas package in a new directory
setup_atlas_repo("ggsegDkt", "dkt")

# Create in current directory, derive name from path
setup_atlas_repo("ggsegMyatlas")

# Specify full path
setup_atlas_repo("~/projects/ggsegSchaefer", "schaefer")

# Without opening in RStudio
setup_atlas_repo("ggsegHarvard", "harvard", open = FALSE)
}
}
