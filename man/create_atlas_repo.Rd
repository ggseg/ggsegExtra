% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-atlas.R
\name{create_atlas_repo}
\alias{create_atlas_repo}
\title{Create a new ggseg atlas package}
\usage{
create_atlas_repo(
  path,
  atlas_name = NULL,
  open = rlang::is_interactive(),
  rstudio = TRUE
)
}
\arguments{
\item{path}{Path where the package should be created. If the path already
exists, it must be an empty directory. If \code{path} is missing and you're
in RStudio, will use the current project directory.}

\item{atlas_name}{Name of the atlas (lowercase, no spaces). The package
will be named \verb{ggseg\{AtlasName\}} (e.g., "ggsegDkt" for \code{atlas_name = "dkt"}).
If not provided, will be derived from the directory name.}

\item{open}{If \code{TRUE}, opens the new project in RStudio (if available).
If \code{NA} (default), opens in RStudio if called interactively.}

\item{rstudio}{If \code{TRUE}, creates an \code{.Rproj} file for RStudio users.}
}
\value{
Invisibly returns the path to the created package.
}
\description{
Creates a new R package structure for a ggseg-compatible brain atlas.
Works from both the R console and the RStudio New Project wizard.
}
\details{
The package includes:
\itemize{
\item Template files for atlas creation (\verb{data-raw/})
\item Documentation templates (\code{R/data.R}, \code{README.Rmd})
\item Test suite for atlas validation (\verb{tests/})
\item GitHub Actions for R CMD check
\item Hex logo creation script
}
}
\examples{
\dontrun{
# Create atlas package in a new directory
create_atlas_repo("ggsegDkt", "dkt")

# Create in current directory, derive name from path
create_atlas_repo("ggsegMyatlas")

# Specify full path
create_atlas_repo("~/projects/ggsegSchaefer", "schaefer")

# Without opening in RStudio
create_atlas_repo("ggsegHarvard", "harvard", open = FALSE)
}
}
