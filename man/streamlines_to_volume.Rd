% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tract-geometry.R
\name{streamlines_to_volume}
\alias{streamlines_to_volume}
\title{Convert streamlines/centerline to volumetric representation}
\usage{
streamlines_to_volume(
  centerline,
  template_file,
  label_value = 1L,
  radius = 2,
  coords_are_voxels = FALSE
)
}
\arguments{
\item{centerline}{Matrix with x, y, z columns}

\item{template_file}{Path to template volume (.mgz, .nii) that
defines the output space}

\item{label_value}{Integer label value to assign to tract voxels (default 1)}

\item{radius}{Dilation radius in voxels to thicken the tract (default 2)}

\item{coords_are_voxels}{Logical. If TRUE, coordinates are
already in voxel space
(1-indexed). If FALSE (default), coordinates are in RAS space and will be
transformed using the volume's vox2ras matrix.}
}
\value{
3D array in RAS+ orientation, tract voxels set to label_value
}
\description{
Creates a 3D volume where voxels containing tract coordinates are labeled.
Uses a template volume to define the output space dimensions and voxel size.
}
\details{
The vox2ras matrix maps to the file's \strong{native} voxel layout, so the
tract volume is built in native orientation, then reoriented to RAS+ at
the end for consistent downstream use.
}
\keyword{internal}
