% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_subcortical_mni152.R
\name{prepare_subcortical_mni152}
\alias{prepare_subcortical_mni152}
\title{Embed MNI152 subcortical parcels in a FreeSurfer aseg for grey-brain context}
\usage{
prepare_subcortical_mni152(
  input_volume,
  labels = NULL,
  lut = NULL,
  replace_labels = aseg_subcortical_labels(),
  target_subject = "fsaverage5",
  registration = NULL,
  output_file = NULL,
  subjects_dir = freesurfer::fs_subj_dir(),
  verbose = get_verbose()
)
}
\arguments{
\item{input_volume}{Path or \code{RNifti} image of the parcellation in FSL-MNI152
space. Only voxels whose value is in \code{labels} are embedded.}

\item{labels}{Integer ids of the parcels to embed. Defaults to every non-zero
id in \code{input_volume}. Ids must not collide with the surviving \code{aseg}
context ids; remap them upstream (e.g. add a fixed offset) if they do.
A collision is an error.}

\item{lut}{Optional colour table (\code{data.frame} with \verb{idx, label, R, G, B, A})
naming the parcels. When \code{NULL}, generic \code{region_XXXX} names and an HCL
palette are generated.}

\item{replace_labels}{Integer \code{aseg} ids the parcels subdivide, blanked
before the parcels are stamped in. Defaults to \code{\link[=aseg_subcortical_labels]{aseg_subcortical_labels()}}.}

\item{target_subject}{FreeSurfer subject whose \code{aseg} supplies the grey-brain
context. Defaults to \code{"fsaverage5"}.}

\item{registration}{Path to the MNI152 registration \code{.dat}. Defaults to
\code{mni152.register.dat} under \code{FREESURFER_HOME/average}.}

\item{output_file}{Optional path for the merged volume; defaults to a
tempfile.}

\item{subjects_dir}{FreeSurfer subjects directory.}

\item{verbose}{Verbosity, passed to the FreeSurfer command runner.}
}
\value{
Invisibly, \code{list(volume, lut)}: the merged volume path and a matching
colour table, ready to pass straight to \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}
(optionally with \code{context = list(focus = ...)}).
}
\description{
Registers a subcortical parcellation supplied in fixed FSL-MNI152 space into
a FreeSurfer subject's \code{aseg}, replacing the lumped aseg structures the
parcels subdivide, so \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}} can render the
parcels as coloured structures inside a grey brain silhouette (cortex, white
matter, cerebellum and brain-stem context).

This is the \emph{fixed-registration} counterpart to
\code{\link[=prepare_subcortical_anatomical]{prepare_subcortical_anatomical()}}. Use this when the atlas already lives in
a standard MNI152 template: the registration is the known
\code{mni152.register.dat} transform (no \code{mri_coreg} search needed) and the
context is taken from \code{fsaverage5}, matching the rest of the ecosystem. Use
\code{\link[=prepare_subcortical_anatomical]{prepare_subcortical_anatomical()}} instead when the registration must be
computed from an arbitrary volume (it targets \code{cvs_avg35_inMNI152} via
\code{mri_coreg}).
}
\examples{
\dontrun{
merged <- prepare_subcortical_mni152(
  input_volume = "BN_Atlas_subcortical_1mm.nii.gz",
  labels = 211:246
)
atlas <- create_subcortical_from_volume(
  input_volume = merged,
  context = list(focus = "region_", match_on = "label")
)
}
}
\seealso{
\code{\link[=prepare_subcortical_anatomical]{prepare_subcortical_anatomical()}} for the computed-registration
(\code{cvs_avg35_inMNI152}, \code{mri_coreg}) counterpart;
\code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}} which consumes the result.
}
