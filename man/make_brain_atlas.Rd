% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-unified-atlas.R
\name{make_brain_atlas}
\alias{make_brain_atlas}
\title{Create brain atlas from FreeSurfer annotation}
\usage{
make_brain_atlas(
  annot,
  subject = "fsaverage5",
  subjects_dir = fs_subj_dir(),
  include_geometry = TRUE,
  output_dir = tempdir(),
  hemisphere = c("rh", "lh"),
  surface = "inflated",
  view = c("lateral", "medial"),
  tolerance = 0,
  smoothness = 5,
  cleanup = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{annot}{Annotation name (e.g., "aparc", "aparc.DKTatlas")}

\item{subject}{FreeSurfer subject (default "fsaverage5")}

\item{subjects_dir}{FreeSurfer subjects directory}

\item{include_geometry}{Whether to create 2D geometry for ggseg plots}

\item{output_dir}{Directory for temporary files (only used if include_geometry = TRUE)}

\item{hemisphere}{Which hemispheres to include}

\item{surface}{Surface for 2D geometry extraction}

\item{view}{Which views to include for 2D rendering}

\item{tolerance}{Tolerance for vertex reduction in 2D polygons}

\item{smoothness}{Smoothing factor for 2D contours}

\item{cleanup}{Remove temporary files after creation}

\item{verbose}{Print progress messages}
}
\value{
A brain_atlas object with vertices, colour, and optionally geometry columns
}
\description{
Creates a brain_atlas object from FreeSurfer annotation files. The atlas
contains vertex indices for 3D rendering and optionally 2D geometry for
ggseg plots.
}
\details{
For 3D-only atlases, this function simply reads the annotation files and
extracts vertex indices and colours - no mesh creation needed. For 2D
geometry, vertex-based rendering is used to take screenshots and extract
contour polygons.
}
\examples{
\dontrun{
# Create 3D-only atlas (fast, no mesh creation)
atlas <- make_brain_atlas(annot = "aparc.DKTatlas", include_geometry = FALSE)
ggseg3d(atlas = atlas)

# Create full atlas with 2D geometry
atlas <- make_brain_atlas(annot = "aparc.DKTatlas")
ggseg(atlas = atlas)
ggseg3d(atlas = atlas)
}
}
