% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/atlas-tracts.R
\name{generate_tube_mesh}
\alias{generate_tube_mesh}
\title{Generate tube mesh from centerline}
\usage{
generate_tube_mesh(centerline, radius = 0.5, segments = 8)
}
\arguments{
\item{centerline}{A matrix with N rows and columns x, y, z defining the
path the tube follows.}

\item{radius}{Tube radius. Either a single value for uniform thickness,
or a vector of length N to vary the radius along the path.}

\item{segments}{Number of segments around the tube circumference. Higher
values make smoother tubes but larger meshes.}
}
\value{
A list with:
\itemize{
\item \code{vertices}: data.frame with x, y, z columns
\item \code{faces}: data.frame with i, j, k columns (1-indexed triangle vertices)
\item metadata: list with n_centerline_points, centerline, tangents
}
}
\description{
Build a 3D tube mesh around a path. The tube follows the centerline with
consistent orientation (no twisting) using parallel transport frames.
Useful for visualising tracts as smooth tubes rather than raw streamlines.
}
\keyword{internal}
