% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create-unified-atlas.R
\name{make_atlas_from_labels}
\alias{make_atlas_from_labels}
\title{Create brain atlas from label files}
\usage{
make_atlas_from_labels(
  label_files,
  atlas_name = NULL,
  type = c("cortical", "subcortical"),
  region_names = NULL,
  colours = NULL,
  subject = "fsaverage5",
  subjects_dir = fs_subj_dir(),
  include_geometry = FALSE,
  output_dir = tempdir(),
  surface = "inflated",
  view = c("lateral", "medial"),
  tolerance = 0,
  smoothness = 5,
  cleanup = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{label_files}{Character vector of paths to .label files}

\item{atlas_name}{Name for the atlas (default: derived from first label file)}

\item{type}{Atlas type: "cortical" or "subcortical"}

\item{region_names}{Optional character vector of region names (default: derived from filenames)}

\item{colours}{Optional character vector of colours for each region}

\item{subject}{FreeSurfer subject (default "fsaverage5")}

\item{subjects_dir}{FreeSurfer subjects directory}

\item{include_geometry}{Whether to create 2D geometry for ggseg plots}

\item{output_dir}{Directory for temporary files (only used if include_geometry = TRUE)}

\item{surface}{Surface for 2D geometry extraction (cortical only)}

\item{view}{Which views to include for 2D rendering}

\item{tolerance}{Tolerance for vertex reduction in 2D polygons}

\item{smoothness}{Smoothing factor for 2D contours}

\item{cleanup}{Remove temporary files after creation}

\item{verbose}{Print progress messages}
}
\value{
A brain_atlas object with vertices, colour, and optionally geometry columns
}
\description{
Creates a brain_atlas object from FreeSurfer .label files. Each label file
represents a single region containing vertex indices. This is useful for
displaying analysis results where specific regions are highlighted.
}
\details{
Label files can contain either cortical surface vertices or subcortical
structure vertices. Set \code{type} accordingly.
}
\examples{
\dontrun{
# Create cortical atlas from label files
labels <- c("lh.region1.label", "lh.region2.label", "rh.region1.label")
atlas <- make_atlas_from_labels(labels)
ggseg3d(atlas = atlas)

# Create subcortical atlas
labels <- c("left_thalamus.label", "right_thalamus.label")
atlas <- make_atlas_from_labels(labels, type = "subcortical")

# With custom names and colours
atlas <- make_atlas_from_labels(
  labels,
  region_names = c("Motor", "Visual", "Motor"),
  colours = c("#FF0000", "#00FF00", "#0000FF")
)
}
}
