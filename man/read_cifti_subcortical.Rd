% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cifti_subcortical.R
\name{read_cifti_subcortical}
\alias{read_cifti_subcortical}
\title{Extract subcortical labels from a CIFTI file}
\usage{
read_cifti_subcortical(cifti_file, output_file = tempfile(fileext = ".nii.gz"))
}
\arguments{
\item{cifti_file}{Path to a \code{.dlabel.nii} CIFTI file with subcortical
voxels.}

\item{output_file}{Path for the NIfTI label volume. Defaults to a temporary
\code{.nii.gz} file.}
}
\value{
A list with \code{volume}, the path to the written NIfTI, and \code{lut}, a
data.frame with columns \code{idx}, \code{label}, \code{R}, \code{G}, \code{B} and \code{A} covering the
labels present in the volume. \code{A} is 0 (opaque), following the FreeSurfer
colour table convention rather than the CIFTI alpha.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

CIFTI dense label files in grayordinate space (such as fsLR 91k) store
subcortical parcels as voxels rather than surface vertices, so
\code{\link[=read_cifti_annotation]{read_cifti_annotation()}} and \code{\link[=create_cortical_from_cifti]{create_cortical_from_cifti()}} cannot use
them. \code{read_cifti_subcortical()} writes those voxels to a NIfTI label volume
and builds the matching colour table from the CIFTI label table.

The volume keeps the CIFTI's voxel grid and transform. Grayordinate files
are in MNI152NLin6Asym (FSL's MNI152), at 2 mm for 91k and 1.6 mm for 170k,
so the result can go to \code{\link[=prepare_subcortical_mni152]{prepare_subcortical_mni152()}} for grey-brain
context, or straight to \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}. Label ids that
reuse FreeSurfer \code{aseg} ids (as HCP-style files often do) must be shifted
before \code{\link[=prepare_subcortical_mni152]{prepare_subcortical_mni152()}}, which refuses colliding ids.
}
\examples{
\dontrun{
subcortex <- read_cifti_subcortical("atlas.dlabel.nii")
merged <- prepare_subcortical_mni152(
  input_volume = subcortex$volume,
  labels = subcortex$lut$idx,
  lut = subcortex$lut
)
atlas <- create_subcortical_from_volume(input_volume = merged)
}
}
\seealso{
\code{\link[=prepare_subcortical_mni152]{prepare_subcortical_mni152()}}, \code{\link[=create_subcortical_from_volume]{create_subcortical_from_volume()}}
}
