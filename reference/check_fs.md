# Check if FS can be run

Check if FS can be run

## Usage

``` r
check_fs(msg = NULL, abort = FALSE)
```

## Arguments

- msg:

  message to print on error

- abort:

  logical. If function should error if Freesurfer is not installed.
  Defaults to FALSE.

## Value

logical
